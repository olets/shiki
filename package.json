{
  "name": "@olets/shiki-monorepo",
  "packageManager": "bun@1.1.25",
  "private": true,
  "scripts": {
    "build": "turbo build check-types",
    "changeset": "changeset",
    "check-types": "tsc --noEmit",
    "publish-packages": "bun run build && bun changeset version && bun changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "turbo": "^2.2.3"
  }
}
